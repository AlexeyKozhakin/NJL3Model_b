# Автоматические тесты

Директория для `pytest`-совместимых тестов. Запуск:

```bash
pytest tests/
```

Или отдельный файл:

```bash
pytest tests/test_b_symmetry.py -v
```

## Структура

- `test_b_symmetry.py` — регрессионная проверка симметрии потенциала относительно $b \to -b$.
