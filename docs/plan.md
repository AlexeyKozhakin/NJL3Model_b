# План работы в ветке `docs-and-verification`

## ✅ Уже сделано
1. Создан `docs/theory.md` с полным теоретическим описанием формул NJL-модели для $\phi = 0$.
2. Найден и исправлен баг со знаками в `integration_limits_plus` (`b>0`) и `integration_limits_minus` (`b<0`).
3. Обнаружен второй баг: при $|b| > M$ и $F_n^{\pm}(0) < 0$ функция имеет **два корня**, но код всегда использует $p_{left} = 0$, пропуская левый корень.
4. Создана библиотека отладочных скриптов в `notebooks/` с визуальными проверками всех режимов.

## 🔄 Следующий шаг: рефакторинг `fun_Omega_L_mu_int`

**Выбранный подход:** numba-цикл по модам (Подход Б).

**Причины:**
- Естественная поддержка любого числа интервалов на моду (включая двухкорневые случаи).
- Простота обобщения на $\phi \neq 0$.
- Потенциальное ускорение за счёт JIT-компиляции и отказа от огромных промежуточных массивов.
- Прозрачность: код будет 1-к-1 соответствовать формулам из `theory.md`.

**Подзадачи:**
1. Написать прототип `@jit(nopython=True)` версии `fun_Omega_L_mu_int` в `notebooks/06_prototype_numba_integration.py`.
2. Сравнить результаты прототипа со старой версией для случаев с одним корнем (регрессионное тестирование).
3. Проверить прототип на случаях с двумя корнями (где старая версия давала ошибку).
4. Перенести исправленную версию в `functions/Omega_mu_L.py`.
5. Запустить полный тест симметрии $b \to -b$ и другие sanity checks.

## 📋 Отложенные задачи
- Уточнить физический смысл параметров $b$ и $L$ (после консультации с научным руководителем).
- Обобщение на $\phi \neq 0$.
- Написать `docs/numerics.md` с описанием midpoint rule, точности и сходимости.
- Создать `pytest`-совместимые автоматические тесты в `tests/`.
