# План работы в ветке `docs-and-verification`

## ✅ Уже сделано
1. Создан `docs/theory.md` с полным теоретическим описанием формул NJL-модели для $\phi = 0$.
2. Найден и исправлен баг со знаками в `integration_limits_plus` (`b>0`) и `integration_limits_minus` (`b<0`).
3. Обнаружен второй баг: при $|b| > M$ и $F_n^{\pm}(0) < 0$ функция имеет **два корня**, но код всегда использует $p_{left} = 0$, пропуская левый корень.
4. Создана библиотека отладочных скриптов в `notebooks/` с визуальными проверками всех режимов.

## ✅ Рефакторинг `fun_Omega_L_mu_int` — ЗАВЕРШЁН

**Выбранный подход:** numba-цикл по модам (Подход Б).

**Сделано:**
- Реализована `@jit(nopython=True)` версия `fun_Omega_L_mu_int` в `functions/Omega_mu_L.py`.
- Поддерживаются случаи с **двумя корнями** ($|b| > M$, $F(0) < 0$).
- Пройдено регрессионное тестирование: результаты для однокорневых случаев совпадают со старой версией.
- Симметрия $b \to -b$ сохраняется на уровне машинной точности.
- Производительность: numba-версия работает мгновенно даже на первом запуске (нет оверхеда на создание огромных промежуточных массивов).

## ✅ Автоматические тесты — ЗАВЕРШЁНЫ

**Сделано:**
- `tests/test_b_symmetry.py` — 18 параметризованных тестов на симметрию $b \to -b$.
- `tests/test_integration_limits.py` — проверка `integration_limits_plus/minus` против аналитических формул.
- `tests/test_two_roots.py` — сравнение numba-реализации с brute-force интегрированием на мелкой сетке.
- В процессе тестирования найдена и исправлена дополнительная ошибка в `_compute_Unm` (`b<0`, один корень).
- Все 30 тестов проходят (`pytest tests/`).

## 🔄 Следующий шаг: на выбор

### Вариант А — `docs/numerics.md`
Написать документацию по численным методам:
- Midpoint rule и его порядок точности.
- Подстановка $p = u/(1-u)$ и её влияние на распределение точек сетки.
- Сеточная сходимость и критерии выбора $N_h$.
- Оценка ошибок округления.

### Вариант Б — Обобщение на $\phi \neq 0$
- Обновить `docs/theory.md` разделом для $\phi \neq 0$.
- Модифицировать `fun_Omega_L_mu_int`: цикл по $n$ от $N_{min}$ до $N_{max}$ без множителя 2.
- Вывести и реализовать формулы для $N_{min}^{\pm}$ и $N_{max}^{\pm}$ при произвольном $\phi$.

### Вариант В — Полноценный запуск `main.py`
- Запустить вычисление на репрезентативной сетке параметров.
- Проверить, что результат выглядит физически разумно (например, выпуклость/вогнутость $\Omega_{ren}(M)$, экстремумы по $M$).
- Визуализировать сечения потенциала.

## 📋 Отложенные задачи
- Уточнить физический смысл параметров $b$ и $L$ (после консультации с научным руководителем).
- Добавить `pytest.ini` или `pyproject.toml` для удобного запуска тестов.
